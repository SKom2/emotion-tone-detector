const Button = () => {
    return (
        <button
            type="submit"
            className="bg-gradient-to-r from-pink-500 to-purple-600 text-white p-2 rounded-lg shadow-lg hover:scale-105 transform transition-transform duration-300"
        >
            Detect Emotional Tone
        </button>
    );
};

export default Button;